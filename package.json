{
  "name": "codex-pet-gallery",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "build": "node scripts/build-gallery.mjs",
    "dev": "npm run build && python3 -m http.server 4173 -d site"
  }
}
